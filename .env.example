# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/fastapi_celery_db
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastapi_celery_db
DB_USER=username
DB_PASSWORD=password

# Test Database Configuration (Separate Port for Isolation)
# Optional: Explicit test database URL (overrides auto-generation)
# TEST_DATABASE_URL=postgresql://username:password@localhost:5433/test_fastapi_celery_db

# Test database settings with separate port
TEST_DB_HOST=localhost
TEST_DB_PORT=5433
TEST_DB_NAME=test_fastapi_celery_db
TEST_DB_USER=username
TEST_DB_PASSWORD=password

# Redis Configuration
REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Test Redis Configuration (Separate DB for Isolation)
TEST_REDIS_URL=redis://localhost:6379/1
TEST_REDIS_DB=1

# Application Settings
DEBUG=True
ENVIRONMENT=development
TESTING=False

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
